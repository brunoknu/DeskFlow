# Copie este arquivo para .env e ajuste os valores para o seu ambiente local.
# O arquivo .env NUNCA deve ser versionado.

# Banco de dados
DB_PASSWORD=Dev@Passw0rd!

# Aplicação
ASPNETCORE_ENVIRONMENT=Development

# E-mail (Mailpit — apenas desenvolvimento)
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_FROM=noreply@deskflow.local
